# The data corpus lives on Google Drive (out of this git repo), so the former
# Git-LFS rules for data/ are no longer needed. See paths.py / environment-notes.md
# for how code locates the data (MFHR_DATA_ROOT). Re-add LFS rules here only if you
# start tracking large binaries in the repo again.
